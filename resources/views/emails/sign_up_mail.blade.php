<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">{{ __('Welcome Onboard') }}</div>

                <div class="card-body">
                    <h1>{{ __('Welcome Onboard.') }}</h1>
                    <p>{{ __('Congratulation') }} {{$details['name']}} {{ __('on becoming the member of 2TopTech as a') }} {{$details['type']}}</p>
                    <p>{{ __('Thank you') }}</p>
                </div>
            </div>
        </div>
    </div>
</div>
